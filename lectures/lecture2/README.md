# FK symbolic (2-DoF planar) and rest-to-rest joint trajectory

In the Jupyter file browser running inside the container, open `fk_symbolic_2dof_planar.ipynb`
and `rest_to_rest_joint_trajectory.ipynb`, then run all cells.
